backup note 260526

Project: new_youtube_downloader_gpt
Date: 2026-05-26 KST

Purpose:
- Add an automatic rename behavior for duplicate filenames.

Change summary:
- Added duplicate policy value "rename" in gui.py.
- Added UI menu label "자동 이름 변경".
- When existing files are detected, the app now chooses the next suffix like " (1)", " (2)" and passes it into the downloader.
- downloader.py appends that suffix before the extension in yt-dlp output templates.
- The suffix path is applied to normal downloads, playlists, Threads downloads, and Naver Blog downloads.

Verification target:
- python -m py_compile gui.py downloader.py main.py

Notes:
- Existing skip / overwrite / ask behavior is kept.
- This change does not regenerate build/dist release artifacts by itself.
